
        <div class="form-claim">
        <div style="float: left; padding: 10px; width: 20%;">
        <big>Claim review steps:</big>
        <div class="divTable blueTable"><div class="divTableBody">
         <% 
          all_steps={'s1'=>'Author credentials','s2'=>'Fact-check history','s3'=>'Quality of previous writing','s4'=>'Connected to biased entities','s5'=>'Difficulty to verify authenticity','s6'=>'Other problems','s7'=>'Calculated score','s8'=>'Reviewer assessment','s9'=>'Sharing setting'};
          passed_step=false
          all_steps.each do |key,value| %>
              <%= is_visible(key).html_safe %><div class="divTableCell">
              <% if (step==File.basename(wizard_path(key)))
                  passed_step=true %>
                  <b>-&gt; <%= value %></b>
              <% elsif passed_step %>
                  <%= link_to value, wizard_path(key), style: 'color:black' %>
              <% else %>
                  <%= link_to value, wizard_path(key), style: 'color:grey' %>
              <% end %>
              </div></div>
         <% end %>
      </div>
      </div>
      </div>
      <div style="float: left; padding: 10px; width: 80%;"
              <h3>Reviewing src:
                <% if (not @src.url.blank?) %>
                   <%= link_to @src.name, src_path(@src.id) %>
                <% else %>
                  <%= @src.name %>
                <% end %>
              </h3> 

      <%= form_for @src_review, method: :put, url: wizard_path do |f| %>
              <% if f.object.errors.any? %>
              <div class="error_messages">
                  <% f.object.errors.full_messages.each do |error| %>
                    <p><%= error %></p>
                  <% end %>
              </div>
              <% end %>
              <fieldset>
                <legend>Sharing your source review</legend>
                  <ul>
                      
                  <li>Do you wish to share your review of this source?<span class='required'>*</span>
                  <%= f.select :src_review_sharing_mode, options_for_select([['Select',''],['Yes', 1],[' No', -1]], :selected => @src_review.src_review_sharing_mode) %>
                  <br>
                  
                  </li>
          
                  <li>Optional note
                  <%= f.text_area(:note_src_review_sharing_mode, size: '50x10', class: 'field-long field-textarea') %>
                  <br>
                  
                  </li>
          
                    <li>
                      <%= link_to 'Previous Step',previous_wizard_path+'?s=prev',:class=>'btn btn btn-primary',:style=>'color:white; background-color:#4B99AD' %>
&nbsp;&nbsp;&nbsp;<%= f.submit 'Finish', :class => 'btn btn btn-primary' %>
                    </li>
                  </ul>
              </fieldset>
              <% end %>
              </div></div>
              