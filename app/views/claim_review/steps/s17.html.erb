
        <div class="form-claim">
        <div class="sidenav">
      <%= form_for @claim_review, method: :put, url: wizard_path do |f| %>
        <big>Claim review steps:</big><br>
        <div class="divTable blueTable"><div class="divTableBody">
         <% 
          passed_step=false
          @all_steps.each do |key,value| %>
              <%= is_visible(key).html_safe %><div class="divTableCell">
              <% if (step==File.basename(wizard_path(key)))
                  passed_step=true %>
                  <b>-&gt; <%= value %></b>
              <% elsif passed_step %>
                  <%= f.submit value, style: 'color:#FFFFFF;width:100%;text-align:left' %>
              <% else %>
                  <%= f.submit value, style: 'color:#C0C0C0;width:100%;text-align:left' %>
              <% end %>
              </div></div>
         <% end %>
      </div>
      </div>
      </div>
      <div class="main">
              <h4>Reviewing claim:
                   <%= link_to @claim.title, claim_path(@claim.id) %></h4>
                <% if (not @claim.url.blank?) %>
                   <%== "URL: <input type='text' id='url_link' value='"+@claim.url.to_s+"' style='background-color:lightgrey;width:75%'> <button onclick=copyToClipboard('#url_link')>Copy</button>" %>
                <% else %>
                   URL: Not provided
                <% end %>
              <br> 
              <% if f.object.errors.any? %>
              <div class="error_messages">
                  <% f.object.errors.full_messages.each do |error| %>
                    <p><%= error %></p>
                  <% end %>
              </div>
              <% end %>
              <fieldset>
                <h5><strong>Reviewing the text content of the claim</strong></h5>
                  <ul>
                      
                  <li>Does the claim include a false statement about crowd sizes or distances, or statistics, names or other information that is just factually wrong?
                  <br><%= f.radio_button :Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?, '-1', :checked => (!@claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?.blank? && @claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?==-1) %> Yes<br><%= f.radio_button :Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?, '1', :checked => (!@claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?.blank? && @claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?==1) %>  No<br><%= f.radio_button :Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?, '0', :checked => (!@claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?.blank? && @claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?==0) %>  Not sure<br><br><%= f.radio_button :Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?, '', :checked => (@claim_review.Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?.blank?) %> Skip this question<br>
                  
                  <br>
                  
                  </li>
          
                  <li>Optional note
                  <%= f.text_area(:note_Did the claim fail fact-checks before since it lacks sufficient verifiable sources or includes dis/misinformation?, size: '50x10', class: 'field-long field-textarea') %>
                  
                  <br>
                  
                  </li>
          
                    <li>
                      
                        <% if !@first_step.present? or @first_step!=step %>
                            <%= f.submit 'Previous Step', :class => 'btn btn btn-primary' %>
                        <% end %>
                            <%= f.submit 'Next Step', :class => 'btn btn btn-primary' %>
                  
                    </li>
                  </ul>
              </fieldset>
              <% end %>
              </div>
              